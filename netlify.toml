[build]
  command = "pnpm install && pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA (TanStack Router / React)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
